version: '3.1'

services:
  db:
    image: "postgres:17.6"
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: somedatabasename
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
