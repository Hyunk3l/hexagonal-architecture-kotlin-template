# Copier Variables

From `copier.yaml`:

- `tld` (default: `com`) - top-level domain for the base package.
- `group_name` (default: `fabridinapoli`) - main package group.
- `project_name` (default: `hexagonalarchitecture`) - project root name and package segment.

Use these to render paths and placeholders in templates.
